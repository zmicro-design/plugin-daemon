#!/bin/bash

help() {
  echo "Remove a daemon service"
  echo ""
  echo "Usage:"
  echo "  zdaemon remove [options] [app_name]"
}

core() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  daemon_remove $@
}

run() {
  core $@
}

run $@
